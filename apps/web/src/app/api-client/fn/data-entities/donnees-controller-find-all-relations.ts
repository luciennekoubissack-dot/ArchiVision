/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DataRelationDetailEntity } from '../../models/data-relation-detail-entity';

export interface DonneesControllerFindAllRelations$Params {

/**
 * Numéro de page, à partir de 1. Absent, la réponse n'est pas paginée (tableau complet).
 */
  page?: number;

/**
 * Nombre d'éléments par page, entre 1 et 200.
 */
  pageSize?: number;
}

export function donneesControllerFindAllRelations(http: HttpClient, rootUrl: string, params?: DonneesControllerFindAllRelations$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DataRelationDetailEntity>>> {
  const rb = new RequestBuilder(rootUrl, donneesControllerFindAllRelations.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DataRelationDetailEntity>>;
    })
  );
}

donneesControllerFindAllRelations.PATH = '/api/v1/data-entities/relations';
