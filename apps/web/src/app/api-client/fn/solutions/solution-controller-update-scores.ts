/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SolutionEntity } from '../../models/solution-entity';
import { UpdateScoresDto } from '../../models/update-scores-dto';

export interface SolutionControllerUpdateScores$Params {
  id: string;
      body: UpdateScoresDto
}

export function solutionControllerUpdateScores(http: HttpClient, rootUrl: string, params: SolutionControllerUpdateScores$Params, context?: HttpContext): Observable<StrictHttpResponse<SolutionEntity>> {
  const rb = new RequestBuilder(rootUrl, solutionControllerUpdateScores.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SolutionEntity>;
    })
  );
}

solutionControllerUpdateScores.PATH = '/api/v1/solutions/{id}/scores';
