/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ConformiteEntity } from '../../models/conformite-entity';

export interface ConformiteControllerFindAll$Params {

/**
 * Numéro de page, à partir de 1. Absent, la réponse n'est pas paginée (tableau complet).
 */
  page?: number;

/**
 * Nombre d'éléments par page, entre 1 et 200.
 */
  pageSize?: number;
}

export function conformiteControllerFindAll(http: HttpClient, rootUrl: string, params?: ConformiteControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ConformiteEntity>>> {
  const rb = new RequestBuilder(rootUrl, conformiteControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ConformiteEntity>>;
    })
  );
}

conformiteControllerFindAll.PATH = '/api/v1/conformites-solutions';
