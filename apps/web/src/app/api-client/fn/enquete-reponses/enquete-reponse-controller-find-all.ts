/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EnqueteReponseEntity } from '../../models/enquete-reponse-entity';

export interface EnqueteReponseControllerFindAll$Params {

/**
 * Numéro de page, à partir de 1. Absent, la réponse n'est pas paginée (tableau complet).
 */
  page?: number;

/**
 * Nombre d'éléments par page, entre 1 et 200.
 */
  pageSize?: number;
}

export function enqueteReponseControllerFindAll(http: HttpClient, rootUrl: string, params?: EnqueteReponseControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<EnqueteReponseEntity>>> {
  const rb = new RequestBuilder(rootUrl, enqueteReponseControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<EnqueteReponseEntity>>;
    })
  );
}

enqueteReponseControllerFindAll.PATH = '/api/v1/enquete-reponses';
